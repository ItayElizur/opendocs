# PP-22 — Verification

**Scope covered:** all 4 tasks. Depended on PP-21's `PptChartTypeMap` correction (done earlier in this same lane, in the same session) before deriving `add_chart.kind`'s enum from it.

## Automated checks — all passed (2026-08-24)

- [x] `npx tsc --noEmit` in `PowerPointAiAddIn/` — clean.
- [x] esbuild bundle rebuild — clean.
- [x] `MSBuild PowerPointAiAddIn/PowerPointAiAddIn.csproj` — clean.

## What was built

- **Task 1 (`add_chart.kind`)**: done as part of PP-21's `AddChartPpt` rewrite in this same session (unknown kind throws, listing valid values from the now-corrected `PptChartTypeMap`; schema enum matches `edit_chart.chartType`'s exactly — verified by reading both schema entries side by side after writing them).
- **Task 2 (`add_smartart.layout`)**: `ResolveSmartArtLayout` now throws on an unknown key (listing the 7 valid keys) and, separately, throws a distinct message when the key is valid but no matching layout name is found in this install's `SmartArtLayouts` gallery — explicitly naming that the install may be non-English. **Step 1's live cross-check was not performed** — no interactive Office GUI access from this environment, same limitation the pre-existing code comment already recorded. Per Step 6, shipped the throwing fixes and the schema enum anyway; the distinct not-found message is now the actual diagnostic path for a mismatched install, which a silent fallback could never provide. Step 4 (index-based lookup) was considered and explicitly declined in a code comment, matching the plan's own recommendation.
- **Task 3 (table parameters)**: `edit_table_structure.kind` gets a real 4-value enum (spellings confirmed against the handler's own switch labels character-for-character, not retyped from memory). Added index bounds validation to all four `insert-row`/`delete-row`/`insert-col`/`delete-col` cases (previously only reachable via a raw COM exception on an out-of-range index) — the existing `default:` case already returned a specific error for an unknown `kind`, so no new fallback needed there. `edit_table_style.borderPreset` widened beyond the plan's minimum ask: implemented `outline` (only the table's outer perimeter) in addition to `all`/`none`, reusing the existing per-cell border loop with a boundary check — cheap enough to include rather than deferring, per the plan's own "if cheap, add it" invitation.
- **Task 4 (sweep)**: read every `{ type: 'string' }` field left in `entry.ts` after the above changes. Every remaining one is genuinely open-ended (hex colors, file paths, free-text content, table cell strings) — none maps to a closed handler set. `add_shape.shapeType` (already enum'd) and `edit_chart.chartType`/`legendPos`/`dataLabels` (PP-21's job) were excluded from the sweep per the plan's own Step 3.

## Deviations from plan

- Task 2 Step 1 (live SmartArt cross-check): not performed, no GUI access — matches the plan's own anticipated fallback (Step 6), not a corner cut.
- Task 3 Step 3: implemented `outline` (beyond the plan's minimum two-value enum) since it was cheap given the existing loop structure.
- `docs/ai-tool-surface.md` (Task 2 Step 2's "record the Office version/locale checked" and Task 4 Step 4's "record the completed list"): **not done** — outside my assigned file list (`PowerPointAiAddIn/PowerPointTools.cs`, `PowerPointAiAddIn/web-src/entry.ts` only). Recorded here instead.

## Manual verification matrix (none of this has been run — no interactive Office session reachable from this environment)

- [ ] `add_chart` with each valid `kind` → the right chart type (particularly `bar` vs `column` after the map fix).
- [ ] `add_chart {kind: 'nonsense'}` → specific error listing valid kinds; no chart added.
- [ ] `add_chart` with a series whose `values` length differs from `categories` → specific error.
- [ ] `add_smartart` with each of the seven layouts → seven visibly different diagrams (this is the check that validates or invalidates the display-name table — genuinely unverified from this environment).
- [ ] `add_smartart {layout: 'nonsense'}` → specific error listing valid layouts.
- [ ] If a layout key resolves to no gallery entry → the distinct "may be a non-English Office install" message, not a silent Basic Block List.
- [ ] `edit_table_structure` with each of the four kinds → correct insert/delete.
- [ ] `edit_table_structure {kind: 'nonsense'}` and an out-of-range `index` → specific errors.
- [ ] `edit_table_style {borderPreset: 'all'|'outline'|'none'}` → correct in each case (`outline` is new — verify it actually draws only the perimeter, not every cell edge); an unknown value → specific error.
- [ ] Natural language: "add an org chart", "add a cycle diagram", "insert a row after row 2" each work on the first attempt.
