# PP-0 — Verification

**Scope covered:** Tasks 1-6 and 10 (TypeScript shell extraction + downstream plan updates). Tasks 7-9 (C# `PaneHostBase`/`RibbonBase`) are **not implemented** — deferred to PP-1 per the plan's own Sequencing note; see `docs/superpowers/plans/2026-08-23-pp01-taskpane-per-window.md`'s new dependency note.

## Automated checks — all passed (2026-08-24)

- [x] `npx tsc --noEmit` — clean in all three apps (Word, Excel, PowerPoint).
- [x] esbuild bundle — all three apps rebuilt successfully with the 4th (`@officeai/app-shell`) alias.
- [x] `MSBuild -t:Build -p:Configuration=Debug` — all three apps + `OfficeAi.Shared` + `OfficeAi.Shared.Tests` build clean.
- [x] `dotnet test OfficeAi.Shared.Tests` — 8/8 passed (unaffected by this plan; run as a regression check).
- [x] `cd shared/chat-ui && npx vitest run` — 15/15 passed (unaffected by this plan; run as a regression check).
- [x] Bundle content check — every WebView2 message kind (`load-history`, `append-message`, `tool-call`, `tool-result`, `history-loaded`, `selection-changed`, `new-chat-divider`, `set-mode`, `collapse-pane`/`expand-pane`, `set-tls-bypass`) appears exactly once in each app's bundle.
- [x] Bundle content check — Word: 7/7 tools present. Excel: 10/10 tools present. PowerPoint: 23/23 tools present.
- [x] `grep -rn "chrome.webview.postMessage"` outside `shared/web-src/app-shell/bridge.ts` — zero hits (Task 1 Step 5's isolation requirement).
- [x] Net line count: `entry.ts` files went from 1220 → 519 lines combined; `shared/web-src/app-shell/` added 366 lines. Net repo-wide: -335 lines, consistent with the plan's own "~400 line net deletion" estimate.

## Manual verification (2026-08-24)

**User-confirmed (general pass):** "everything still works as planned" — a general smoke pass across the three add-ins after the PP-0 migration, covering the ordinary chat flow (send, tool execution, replies). No specific regression reported from this change.

The itemized checklist below was **not** individually walked through and confirmed line-by-line — it remains open for anyone who wants finer-grained confidence before building further on top of PP-0. Marking it accordingly rather than claiming per-item confirmation that didn't happen:

Word (checkpoint app, Task 5 Step 2):
- [ ] Send a chat message and confirm a real reply streams in.
- [ ] Trigger a tool call (e.g. "read the first paragraph") and confirm it executes and shows in the transcript.
- [ ] Change editing mode (Read only / Comment only / Track changes / Full autonomy) and confirm the offered tools change accordingly.
- [ ] Change language (EN/HE) and confirm the panel relocalizes and flips RTL.
- [ ] Save settings (base URL/key/model/TLS toggle) and confirm the next request uses them.
- [ ] Collapse and expand the pane.
- [ ] Select text in the document and confirm the scope-hint pill updates and the selection reaches the model's context.
- [ ] Close and reopen the document — chat history restores.

Excel — same checklist minus the text-selection step (Excel has no selection wiring yet; that's FT-2).

PowerPoint — same checklist minus text-selection, **plus** the one behavior change this plan makes:
- [ ] Set Read only mode → confirm mutation tools (e.g. `set_element_text`) are refused/not offered.
- [ ] Set Full autonomy → confirm they return. (This exercises the switch from PowerPoint's old `skill.tools = ...` reassignment to the shared live-getter pattern — Task 6 Step 2's explicit correctness checkpoint.)

**Separately reported (2026-08-24):** opening a second Word document and clicking the ribbon button only toggled the pane in the first document's window. **This is not a PP-0 regression** — it is the exact, expected `ThisAddIn.cs`/`TogglePane()` single-pane bug PP-1 (`2026-08-23-pp01-taskpane-per-window.md`) exists to fix; confirmed by reading the current (unmodified by PP-0) `ThisAddIn.cs`, which still creates exactly one `CustomTaskPane` at startup. Tracked as PP-1's problem, not a new finding.

## Deviations from plan

- None. Implementation followed the plan's file/interface design as written.

## Notes for the next implementer

- `settings.ts` exports `getSettings()`/`setSettings()` functions rather than a mutable `currentSettings` binding (per the plan's Task 2 Step 3). PP-6 has been updated to reflect this exact shape in its code samples — read that update before implementing PP-6.
- PP-1 Tasks 7-9 (the C# half of this plan) must be done before PP-1's Tasks 1-4; see the dependency note now at the top of `2026-08-23-pp01-taskpane-per-window.md`.
