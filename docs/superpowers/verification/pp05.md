# PP-5 — Verification

**Scope covered:** all 5 tasks in full — structural per-kind schemas for both gateway tools, the runtime required-field precheck in both C# handlers, the exhaustive schema-vs-handler cross-check, and the schema-size measurement with the grouped-variant fallback.

## Automated checks — all passed (2026-08-24)

- [x] `npx tsc --noEmit` in `WordAiAddIn/` — clean.
- [x] `npx tsc --noEmit` in `ExcelAiAddIn/` — clean.
- [x] esbuild bundle rebuilds for both apps (4-alias command).
- [x] `MSBuild WordAiAddIn/WordAiAddIn.csproj` — clean.
- [x] `MSBuild ExcelAiAddIn/ExcelAiAddIn.csproj` — clean.
- [x] Exhaustive cross-check (Task 3): every `case` in `WordTools.cs`'s `ApplyCommands` switch (12) has exactly one match in `WORD_COMMAND_SCHEMAS`, and vice versa — diff empty. Same for `ExcelTools.cs`'s `ProposeOperations` switch (51) against `EXCEL_OPS` — diff empty. Also cross-checked both C# `RequiredFields` dictionaries' keys are a subset of their respective handler kind sets (no typos) — both clean.
- [x] Schema-size measurement (Task 5), using `tiktoken`'s `cl100k_base` encoding on the actual generated `ALL_TOOLS` (via a stubbed `@officeai/app-shell` import, not a hand-estimate): full-detail (all 51 Excel kinds with `oneOf`) measured **+4,870 tokens** over the ~4k threshold → switched to the grouped variant (`DETAILED_KINDS`, 7 high-ambiguity kinds detailed, the rest collapsed to a `kind` enum) → remeasured at **+2,120 tokens**. Both the trigger and the fallback are in `entry.ts`'s comments, not just this file.

## What was built

- `WordAiAddIn/web-src/entry.ts`: `TARGET_SCHEMA`, `TEXT_STYLE_SCHEMA`/`TEXT_STYLE_FIELDS`, `PARAGRAPH_STYLE_SCHEMA`/`PARAGRAPH_STYLE_FIELDS`, `IMAGE_PROPERTIES_SCHEMA`/`IMAGE_PROPERTIES_FIELDS`, `WORD_COMMAND_SCHEMAS` (12 branches, one per `ApplyCommands` case). `apply_commands`'s `inputSchema` now uses `oneOf: WORD_COMMAND_SCHEMAS`. `highlight` deliberately absent from `TEXT_STYLE_SCHEMA` (PP-12's job).
- `WordAiAddIn/WordTools.cs`: `RequiredFields` dictionary (12 entries) + `ValidateRequired`, called right after `kind` is parsed — which itself moved inside the per-command `try` (was `cmd.GetProperty("kind")` outside it). Catch block now uses `kind ?? "(unknown kind)"` for the missing-kind case.
- `ExcelAiAddIn/web-src/entry.ts`: `OpSpec` interface, `CF_FORMAT_SCHEMA`/`CF_RULE_SCHEMA` (8 conditional-format rule kinds), `DATA_VALIDATION_SCHEMA` (5 kinds; `checkbox` deliberately excluded — the handler always throws for it), `EXCEL_OPS` (51 entries, one per `ProposeOperations` case), `opSchemas`/`propsSummary`/`opsDescription` generators, `DETAILED_KINDS` grouped-variant split.
- `ExcelAiAddIn/ExcelTools.cs`: `RequiredFields` dictionary (45 entries — the 6 kinds needing nothing beyond `kind` have no entry, matching Word's convention) + `ValidateRequired`, same `kind`-inside-try-catch move as Word.
- `docs/ai-tool-surface.md`: added an update note at the top of "Schema-vs-implementation audit" recording what PP-5 fixed (the structural schema problem + per-command isolation), the cross-check result, the token measurement, and explicitly what it did *not* fix (the specific capability/silent-fallback findings below it, each owned by its own later PP item) — the original audit tables are left as a dated record, not rewritten.

## Deviations from plan

- None of substance. The plan's Task 2 Step 4 generator design (`opSchemas`/`opsDescription` from one `EXCEL_OPS` source) is implemented essentially as specified; `propsSummary` is an internal helper not named in the plan but is exactly the "derive the prose from the same source" mechanism Step 4 asked for.
- `updateParagraphStyle`'s and `updateImageProperties`'s exact per-field schemas were not given verbatim in the plan text (only `updateTextStyle`'s was) — derived directly from `UpdateParagraphStyle`/`UpdateImageProperties`'s `fields.Contains(...)` checks in `WordTools.cs`, per the plan's own instruction to "read each private method rather than trusting the current description." Cross-checked against the source a second time before finalizing.

## Everything noticed but not fixed (belongs to other PP items, not touched)

- Word: `highlight` unimplemented in `UpdateTextStyle` (PP-12); `bulletPreset` only distinguishes `BULLET`/anything-starting-with-`NUMBERED` (PP-12); `updateParagraphStyle`'s `borders` field is a blunt boolean (all four sides on/off) with no per-side or style control — not previously documented as a finding, noting it here for whoever eventually revisits paragraph styling.
- Excel: `add_chart.chartType` narrower than `edit_chart.chartType` (PP-15); `add_conditional_format`'s `number`/`text`/`duplicate` kinds have narrower operator/mode support than Excel natively offers (PP-14); `add_shape.shapeType`'s 26-name vocabulary (PP-16); `set_page_setup`'s `scale`+`fitToWidth` mutual-exclusivity silent-drop (PP-18); `delete_table`'s `Unlist()` keeping all data despite the name (PP-18); `add_sparkline`'s `targetCell` defaulting onto its own source range (PP-18); `add_defined_name`/`delete_defined_name` being workbook-scope-only (PP-17). All of these are schema-accurate now (the enum states the narrow truth) — none are schema/handler mismatches anymore, just capability gaps for their respective PP items to close.
- Not fixed and explicitly out of scope for this plan per its Global Constraints: no attempt at transactional rollback for a partially-applied batch (Task 4 Step 4 explicitly says not to — Word/Excel COM offers no batch transaction).

## Manual verification matrix (none of this has been run — no interactive Office session reachable from this environment)

- [ ] Word: send `apply_commands` with a well-formed `updateTextStyle` targeting a heading by `containsText` — bold+italic apply correctly.
- [ ] Word: send a batch `[valid set_bold, {no "kind" field}, valid find_replace]` — commands 1 and 3 apply, command 2's result line reads `(unknown kind): ERROR - Command is missing a string "kind" field.`, and the overall result's `IsError` is true.
- [ ] Word: send `updateTextStyle` with `target`/`style` but no `fields` — result names `fields` specifically as the missing field.
- [ ] Word: ask for a bulleted list via natural language — the model should now produce a `createParagraphBullets` command shaped correctly on the first attempt (schema visible), where before it had only prose to go on.
- [ ] Excel: send `propose_operations` with a well-formed `add_conditional_format` using the `number`/`between` rule kind, including `value2` — verify it actually creates a between-range rule (this was the single worst-documented case before PP-5).
- [ ] Excel: send a batch `[valid set_cell, {kind: "format_range", no address}, valid clear_range]` — commands 1 and 3 apply, command 2 reports `Operation "format_range" is missing required field "address".`, overall `IsError` true.
- [ ] Excel: ask the model to add a star shape (`add_shape`) via natural language — with the full 27-name enum now visible in the *detailed* schema branch, this should work on the first attempt.
- [ ] Excel: ask for an operation kind that falls in the **collapsed** branch (e.g. `set_row_height`) — confirm the model still produces a correctly-shaped call from the generated prose description alone (this is the one path where the model has no structural schema to lean on, only text — worth specifically confirming it still works).
- [ ] Both apps: confirm the full tool-list request size is comfortably within the configured provider's context/tool-size limits in practice (the token measurement above is a proxy, not a live provider test).
