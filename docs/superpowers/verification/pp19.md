# PP-19 (Task 1 only) — Verification

**Scope covered:** Task 1 only — `delete_slide`, `move_slide`, `duplicate_slide`. Task 2 (the scripting-DSL/deck-generation scope decision) was explicitly not touched; it remains a product-owner decision gate, tracked separately in `docs/superpowers/plans/STATUS.md`'s Decisions table.

## Automated checks — all passed (2026-08-24)

- [x] `npx tsc --noEmit` in `PowerPointAiAddIn/` — clean.
- [x] esbuild bundle rebuild — clean.
- [x] `MSBuild PowerPointAiAddIn/PowerPointAiAddIn.csproj` — clean.

## What was built

- `PowerPointTools.cs`: `DeleteSlide`, `MoveSlide`, `DuplicateSlide`, registered in `Execute`'s switch. `DeleteSlide` rejects an out-of-range index and rejects deleting the last remaining slide; its output states the index-shift explicitly (slides after it move down by one). Deliberately no batch `slideIndexes:number[]` form, per the plan's Step 2 — one slide per call, forcing a re-read between deletes in the same run.
- `entry.ts`: three new tool schemas in `MUTATION_TOOLS` (so none is offered in Read Only/Comment Only mode — confirmed via `READER_TOOLS.map((t) => t.name)`, which the three new tools are not part of). System prompt updated to mention them.
- `docs/ai-tool-surface.md`: **not updated** — Step 8 asked for this and for removing `delete_slide` from the out-of-scope list, but `docs/ai-tool-surface.md` is a large shared reference file also being edited by PP-5 (Phase 3, landed) and potentially other lanes; I did not touch it to avoid an unreviewed edit to a file outside my assigned scope. Flagging this as **not done** — worth a follow-up edit by whoever owns that file next.

## Deviations from plan

- None of substance in the C#/TS work itself.
- Step 8 (docs/ai-tool-surface.md update) skipped, per above — outside my file list (`PowerPointAiAddIn/PowerPointTools.cs`, `PowerPointAiAddIn/web-src/entry.ts` only).

## Confirmed not touched (per directive)

- PP-19 Task 2 (scope decision) — not touched.
- PP-20 (style/shape breadth, depends on Excel lane's PP-16) — not touched, not even partially.

## Manual verification matrix (none of this has been run — no interactive Office session reachable from this environment)

- [ ] `delete_slide {slideIndex: 1}` on a 3-slide deck → slide 2 gone, 2 slides remain, output states the index shift.
- [ ] `delete_slide` on the only slide → specific error, slide untouched.
- [ ] `delete_slide {slideIndex: 99}` → specific out-of-range error naming the valid range.
- [ ] `move_slide {slideIndex: 0, toIndex: 2}` → the first slide becomes the third.
- [ ] `duplicate_slide {slideIndex: 0}` → a content-identical copy at position 2.
- [ ] `add_slide` still behaves as before (layout clone, optional text clear) — no regression.
- [ ] In Read only mode → none of the three is offered and a direct call is refused.
- [ ] Natural language: "delete the third slide" and "move the summary to the end" each work on the first attempt.
