# PP-3 — Verification

**Scope covered:** Tasks 1-3 (core feature + styling + tests). **Task 4 (optional input-display polish) was skipped** — the plan explicitly marks it optional ("Skip this task if scope needs trimming; it does not block PP-3's goal"), and it wasn't needed to satisfy the plan's stated goal.

## Automated checks — all passed (2026-08-24)

- [x] `cd shared/chat-ui && npx vitest run` — all 6 PP-3 tests pass (output rendering + disclosure toggle, XSS guard, truncation + show-all, error-expanded-by-default, empty-output-stays-quiet), as part of the combined 26/26.
- [x] `npx tsc --noEmit` — clean in all three apps.
- [x] esbuild bundle — all three apps rebuilt successfully.
- [x] `MSBuild -t:Build` — Excel and PowerPoint build clean. Word currently fails for an unrelated reason (Track B's in-progress C# refactor — see `pp02.md`'s note; identical situation, not re-explained here).

## Task-by-task

- **Task 1**: `TOOL_OUTPUT_PREVIEW_CHARS = 2000`, the `<pre class="ai-step-output" dir="auto">` element, population in `complete()` with truncation + "show all" button (with `stopPropagation`), per-row disclosure via the title click, and error rows starting expanded — all implemented exactly per the plan's code samples.
- **Task 2** (styling): added rules for `.ai-step-output` (monospace, pre-wrap, word-break, max-height/overflow, background+border from existing tokens), a disclosure caret on `.ai-step-title` (reusing the `.ai-work-group-summary` caret pattern, including RTL rotation), `.ai-step-output.error` using the same `--docs-ai-error` token `.ai-step-icon.error` already uses, and `.ai-step-output-more` as a quiet inline text button.
  - **Deviation, flagged explicitly:** removed the pre-existing, unused `.ai-tool-output`/`[dir='rtl'] .ai-tool-output` CSS rules that sat in exactly the spot Task 2 said to add the new rules. Confirmed via grep that `chat-ui.ts` never referenced that class (only `.ai-applied-tag` was used) — it was vestigial, evidently left over from matching `shared/chat-ui/mockup.html`'s design, which still has its own independent copy of that rule (mockup.html is a standalone file with inline `<style>`, not importing `chat-ui.css`, so nothing broke). Keeping both the old dead rule and the new correctly-named one would have been confusing. If this removal is unwanted, it's a one-rule revert.
  - Dark theme: no separate dark-mode rules were needed — every new rule uses `var(--color-...)` tokens exclusively, so both themes follow automatically through the existing token system (per the plan's Step 4 instruction to match, not duplicate, the existing single-declaration-set pattern).
- **Task 3** (tests): all 6 specified cases added and passing, including the XSS regression guard (`<img src=x onerror=alert(1)>` renders as literal text, no `img` element created).

## Deferred to manual verification (no interactive Office session available)

Task 5's manual items — none of these were run:
- [ ] Word: ask a question triggering `get_document_context`; expand the step; confirm paragraph/word counts and preview text are visible and match the document. **This is the check that actually resolves the "get_document_context returns empty" report** — either the output is there (tool was fine, visibility was the bug) or it's genuinely empty (a real `WordTools` bug to file separately). I could not run this.
- [ ] Excel: `read_range` over ~200 cells — confirm truncation + "show all" works and the panel stays scrollable.
- [ ] Force a tool error (e.g. `read_blocks` with an out-of-range index) and confirm the error text is visible without clicking anything.

Task 5 Step 1 (wire-through check) was done by inspection rather than a live tool call: `execution.output` flows from C# `ToolResult.Output` → `ToolProtocol.SerializeToolResult` → the WebView2 message → `bridge.ts`'s `callDotNetTool` resolution → `bootstrap.ts`'s `onToolExecuted` → `complete({ output: event.execution.output, ... })` unchanged from before PP-0/PP-2/PP-3 — no drop point found in the `ToolExecution` type or anywhere in that chain. This is a read of the existing, unmodified pipeline, not a new claim.

## Deviations from plan

- Task 4 skipped (explicitly optional).
- CSS cleanup beyond Task 2's literal instructions — see above, flagged as a deviation worth a look.
