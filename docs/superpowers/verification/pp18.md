# PP-18 — Verification

**Scope:** all 3 findings fixed. `set_page_setup`: scale+fit now explicitly conflict-checked (error, not silent scale-drop); the `fitToHeight`-only path now correctly clears `Zoom` too (previously a third, undocumented behavior); `scale` range-validated 10-400; `fitToHeight: 0` means unlimited-tall (`FitToPagesTall = false`). `delete_table`: default behavior unchanged (Unlist, keeps data) but now documented and reported accurately; `deleteData: true` + `shift: up|left|none` actually removes cells, shift validated *before* `Unlist()` so a bad value leaves the table untouched. `add_sparkline`: `targetCell` is now required (no more defaulting onto its own source data); shape validated (single cell or row-count match) with a specific error; `type` throws on an unknown value instead of falling back to line.

**Automated checks:** `npx tsc --noEmit` clean; `MSBuild ExcelAiAddIn/ExcelAiAddIn.csproj` clean (confirms `XlDeleteShiftDirection.xlShiftToLeft`/`xlShiftUp` exist in this PIA).

**Deviations:** none of substance. `add_sparkline`'s returned "target=..." detail is the target address, not a separate stable sparkline-group id — the plan itself anticipated this PIA might expose no such id and said to document that rather than guess; there is also no `edit_sparkline`/`delete_sparkline` operation yet to consume an id anyway.

## Manual verification matrix (none of this has been run — no interactive Office session reachable from here)

- [ ] `{scale: 80}` alone → 80% scale.
- [ ] `{fitToWidth: 1}` alone → fits one page wide.
- [ ] `{fitToHeight: 2}` alone → fits two pages tall (previously silently broken — Zoom was never cleared on this path).
- [ ] `{fitToWidth: 1, fitToHeight: 0}` → one page wide, unlimited tall.
- [ ] `{scale: 80, fitToWidth: 1}` → specific error; setup unchanged.
- [ ] `{scale: 5}` / `{scale: 900}` → specific range errors.
- [ ] `delete_table` default → table gone from the Name Box, data still present.
- [ ] `delete_table {deleteData: true}` → data gone, rows shifted up.
- [ ] `delete_table {deleteData: true, shift: 'none'}` → cells emptied in place.
- [ ] `delete_table {deleteData: true, shift: 'nonsense'}` → specific error; table still intact (not partially unlisted).
- [ ] `add_sparkline` without `targetCell` → specific error, nothing drawn.
- [ ] `add_sparkline` with `targetCell` == `dataRange` → specific overlap error.
- [ ] `add_sparkline` with a valid, non-overlapping `targetCell` → sparkline in that cell, source data still readable.
- [ ] `add_sparkline` with a mismatched target shape (e.g. 2-cell target for 5-row data) → specific error.
- [ ] `add_sparkline {type: 'nonsense'}` → specific error listing valid types.
