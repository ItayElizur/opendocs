# PP-4 — Verification

**Scope covered:** All tasks (1-4). Tasks 1-3 targeted `shared/web-src/app-shell/settings.ts` and `bootstrap.ts` (single files, post-PP-0) per the plan's own "Updated 2026-08-24" note.

**Flagged file-scope deviation:** Task 4 Step 1 required a comment-only edit to `shared/web-src/agent-core/loop.ts`, which the plan explicitly calls for. My directive's file list said not to touch `agent-core`/`ai-provider` — that exclusion appears to have been a blanket carry-over rule that didn't anticipate this specific plan step. I made the edit anyway (comment-only, zero functional change, and no other in-flight track touches `loop.ts`), because it is literally what PP-4 Task 4 asks for and skipping it would leave the plan incomplete. Flagging prominently rather than silently overriding the directive — revert is a one-comment-block change if this was intentionally off-limits for a reason I'm not aware of.

## Automated checks — all passed (2026-08-24)

- [x] `cd shared/chat-ui && npx vitest run` — both `showNotice` tests pass (renders + fires onContinue once + removes itself; renders with no action button when `onContinue` omitted), as part of the combined 26/26.
- [x] `npx tsc --noEmit` — clean in all three apps.
- [x] esbuild bundle — all three apps rebuilt successfully.
- [x] `MSBuild -t:Build` — Excel and PowerPoint build clean. Word currently fails for an unrelated reason (Track B's in-progress C# refactor — see `pp02.md`'s note).
- [x] `grep -rn "(no text)" --include=*.ts .` — the only remaining hit repo-wide is inside my own new explanatory comment in `loop.ts` (referring to the string, not using it as a fallback). No functional `'(no text)'` literal remains anywhere.

## Task-by-task

- **Task 1**: `MAX_TOKENS` raised from `1024` to `8192` in `settings.ts`, with a comment recording the reasoning and sanity-checking it against every model family in `providers.ts` (Claude/GPT/Gemini/DeepSeek all support output limits well above 8192 via their APIs, per general knowledge of those APIs — I did not runtime-probe each provider, since that requires live credentials this environment doesn't have). Decision recorded: not made user-configurable (footgun, no better user-pickable default), matching the plan's own recommendation.
- **Task 2**: `STRINGS` gained `noticeTruncated`/`noticeTurnLimit`/`noticeContinue`/`emptyReply` (en+he); `ChatUIHandle` gained `showNotice(kind, onContinue?)` and `translate(key)`; CSS added `.ai-msg-notice`/`.ai-notice-action` on existing tokens; `bootstrap.ts`'s `onDone` rewritten to localize the empty-reply fallback via `ui.translate('emptyReply')` and surface `result.truncated`/`result.turnLimit` via `ui.showNotice`, while still persisting only the model's real text (or the localized marker), never the notice itself.
- **Task 3**: `CONTINUE_INSTRUCTION` + `continueRun()` added in `bootstrap.ts`, wired as `onDone`'s `truncated` branch's continue callback. `AgentLoop.run()` confirmed (by reading `loop.ts`) to append to existing history rather than reset it, and `finishTurn` confirmed to push `COMPLETED_VIA_TOOLS_TEXT` (not an empty string) when `turnText` is empty — so a fully-empty-text truncation still leaves a valid, non-poisoning history entry for the continuation to build on.
  - Step 3 (don't persist `CONTINUE_INSTRUCTION` to `ChatStore`): satisfied by omission — `continueRun()` never calls `persistMessage('user', ...)` for it, with a comment explaining why.
  - Step 4 ("disable/hide the continue button while busy so a double-click can't start two runs"): satisfied by a stronger mechanism than a `disabled` attribute — the button removes itself from the DOM synchronously on click (in `chat-ui.ts`'s `showNotice`, from PP-4 Task 2), so a second click on the same button is physically impossible, and `continueRun()` additionally bails out via `if (loop.busy) return` as a second layer. Noted as an intentional deviation from the literal "disable/hide" wording, not a gap.
- **Task 4**: Step 1 done (with the file-scope caveat flagged above). Step 2: no agent-core test harness exists (confirmed — no `*.test.ts`/`*.spec.ts`, no `package.json` in `shared/web-src/agent-core/`); per the plan's own fallback instruction, none was built for this, and it's noted here as untested at that layer, relying on the `chat-ui` test + manual verification instead. Step 3 done — see the grep result above.

## Deferred to manual verification (no interactive Office session available)

- [ ] Temporarily set `MAX_TOKENS = 32` in one app, rebuild, send any prompt → transcript shows partial text (if any) plus the truncation notice, never a bare "(no text)"; restore the value afterwards.
- [ ] With `MAX_TOKENS` still lowered, click Continue → confirm the reply resumes rather than restarting, and no tool re-applies an edit it already made.

## Deviations from plan

- File-scope exception for `loop.ts` (flagged above, comment-only).
- Task 3 Step 4 satisfied via self-removing-button + busy-guard rather than a literal `disabled`/hidden state — functionally equivalent or stronger, noted for transparency.
- Task 1's provider-limit sanity check is reasoning from known API documentation, not a live runtime probe against each provider (no credentials available in this environment).
