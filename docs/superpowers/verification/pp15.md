# PP-15 — Verification

**Scope:** all 6 tasks done (Task 5's `=ref` passthrough required no code change - `Series.Name`'s COM setter already treats a leading `=` as a formula reference, confirmed by reading `EditChartExcel`'s existing `series.Name = nameEl.GetString()` line; documented in the schema instead). `AddChart` now uses `ExcelChartTypeMap` (widened with `columnStacked`/`barStacked`) instead of a 3-way ternary, throws on unknown `chartType`, and returns the chart's name (optional `name` param to set it explicitly). `EditChartExcel` gained rebinding (`dataRange`+optional `dataSheet`+`plotBy`, applied before the type change, per the plan's own ordering requirement) and now throws instead of silently skipping an unrecognized `chartType`. Schema widened to match on both `add_chart` and `edit_chart`, including documenting the rebind-then-restyle ordering and that legend/colors/labels are `edit_chart`-only.

**Automated checks:** `npx tsc --noEmit` clean; `MSBuild ExcelAiAddIn/ExcelAiAddIn.csproj` clean (confirms `Excel.XlRowCol.xlRows`/`xlColumns` exist in this PIA).

**Deviations:** `scatter`/`radar` were considered (plan Task 1 Step 3) and deliberately **not added** — the plan itself flags scatter's different data-shape requirements as something to verify before adding, and that can't be done without real Excel from here; left out per the plan's own fallback instruction rather than guessing.

## Manual verification matrix (none of this has been run — no interactive Office session reachable from here)

- [ ] `add_chart` with each of the 8 types produces that type.
- [ ] `add_chart {chartType: 'nonsense'}` → specific error; no chart created.
- [ ] `edit_chart` changes an existing chart's type across several of the 8 values.
- [ ] `edit_chart` with `dataRange` extended by new rows → chart shows the new rows, title/legend preserved.
- [ ] `edit_chart` with `dataRange` + `plotBy: 'rows'` → series/categories swap as expected.
- [ ] `edit_chart` with `dataSheet` pointing at another sheet → chart plots that sheet's data.
- [ ] `add_chart` returns a name; the next operation in the same batch edits that chart successfully.
- [ ] `add_chart` with an explicit `name` → usable immediately.
- [ ] `seriesData` with a literal name and with an `=Sheet1!$B$1` reference both work, the reference updating live when the cell changes.
- [ ] Natural language: "make this a horizontal bar chart" and "update the chart to include the new rows" both work on the first attempt (rebinding, not a duplicate chart).
