# PP-16 — Verification

**Scope:** all 4 tasks done. Task 1 (export vocabulary to schema) was already done by PP-5 — verified: `EXCEL_OPS`'s `add_shape` entry already carries the full 27-value enum (26 `ShapeTypeMap` keys + `textbox`), correctly counted and cross-checked against the map. `edit_shape` correctly has no shapeType field (the handler `EditShapeExcel` never accepted one). Task 2: `ShapeTypeMap` is now `StringComparer.OrdinalIgnoreCase`; `AddShapeExcel` throws a specific error (built from `ShapeTypeMap.Keys`, so it can't drift) instead of silently falling back to a rectangle. Task 3: `AddShapeExcel` now returns the shape's name (optionally settable via a new `name` param), and `ProposeOperations`'s `add_shape` case surfaces it in the result line instead of a bare "ok".

**Automated checks:** `npx tsc --noEmit` clean; `MSBuild ExcelAiAddIn/ExcelAiAddIn.csproj` clean.

**Deviations:** none.

**Note for the PowerPoint lane:** this plan is done and this file confirms it — PP-20 (PowerPoint's `add_shape` widening, which reuses this map) is unblocked.

## Manual verification matrix (none of this has been run — no interactive Office session reachable from here)

- [ ] Each of the 26 presets + textbox produces the correct shape in real Excel.
- [ ] `shapeType: 'nonsense'` → specific error listing every valid name; no shape created.
- [ ] `shapeType: 'STAR5'` (wrong case) → still works.
- [ ] `add_shape` returns a usable name; `edit_shape` with it succeeds in the same batch.
- [ ] `add_shape` with an explicit `name` → that name is set and usable immediately.
- [ ] Natural language: "add a star", "add an arrow pointing right", "add a heart" each work on the first attempt.
- [ ] `fillColor`/`text` still apply as before on every shape type.
