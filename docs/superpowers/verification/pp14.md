# PP-14 — Verification

**Scope:** Task 1 was already mostly done by PP-5's `CF_RULE_SCHEMA`, but that version documented the CURRENT (narrow) truth, not the widened target — verified by reading it before starting, then widened it as part of this plan: `number.operator` enum went from 4 to all 8 comparisons, `text` gained `match`, `duplicate` gained `mode`, `formula` gained the anchor-cell-relative note. Tasks 2-5 done in `ExcelTools.cs`: `MapCfOperator` now covers all 8 operators and throws on unknown instead of defaulting to `xlEqual`; `value`/`value2` go through a new `GetCfNumber` helper (accepts number or numeric string) with an explicit required-check for between/notBetween; a new `MapCfTextMatch` covers all 4 `XlContainsOperator` modes; `duplicate`'s `mode` field flips `DupeUnique` between `xlDuplicate`/`xlUnique`; the switch gained a `default: throw` (previously fell through silently reporting `ok`); `range`/`rule`/`kind` are read via `TryGetProperty` with specific error messages instead of `GetProperty`'s bare exceptions; `AddConditionalFormat` now returns a description string (kind, range, effective operator/mode) that the batch line appends instead of a bare `ok`.

**Automated checks:** `MSBuild ExcelAiAddIn/ExcelAiAddIn.csproj` clean (confirms `xlNotEqual`/`xlGreaterEqual`/`xlLessEqual`/`xlNotBetween` and all 4 `XlContainsOperator` members exist in this project's PIA - no fallback/omission needed, unlike the `MsoAutoShapeType`/`XlDVType` precedents this plan's Global Constraints anticipated); `npx tsc --noEmit` clean.

**Deviations:** none of substance.

## Manual verification matrix (none of this has been run — no interactive Office session reachable from here)

- [ ] All 8 `number` operators, including the 4 new ones, checked in Excel's Manage Rules dialog.
- [ ] `between`/`notBetween` with `value2`; without it → specific error naming the operator.
- [ ] All 4 text `match` modes, especially `notContains` — the request the source item names as currently impossible.
- [ ] `duplicate` with `mode: 'unique'` — the second named-impossible request.
- [ ] Unknown `kind`/`operator`/`match`/`mode` each → specific error naming valid values; no rule created.
- [ ] `format` applies on number/text/blank/duplicate/top10/formula; colorScale/dataBar unaffected (no format block reached, as before).
- [ ] Missing `kind`/`range`/`rule`, and a bad range address → specific errors, not raw exceptions.
- [ ] Result line for a successful rule shows the new descriptive detail (e.g. "number range=B2:B50 (number notEqual 0)") instead of a bare "ok".
- [ ] Natural language: "highlight cells in B2:B50 that don't contain 'approved' in red" succeeds on the first attempt.
