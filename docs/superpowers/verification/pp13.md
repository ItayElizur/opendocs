# PP-13 — Verification

**Scope:** all 5 tasks done. `format_range` widened from bold/italic/numberFormat/fillColor to full parity (fontName/fontSize/fontColor/strikethrough/underline enum-or-bool/horizontalAlignment/verticalAlignment/wrapText/textRotation/indent/borders). `read_formats` widened to match, with the "explicitly formatted" filter extended to the new properties. Schema (`EXCEL_OPS`'s `format_range` entry, already in `DETAILED_KINDS` from PP-5) updated with real enums and range documentation.

**Automated checks:** `npx tsc --noEmit` clean; `MSBuild ExcelAiAddIn/ExcelAiAddIn.csproj` clean.

**Deviations:** none of substance. `ApplyBorders`/`FormatRange` return a `string` note (not `void`) so the single-cell interior-border skip (Task 2 Step 4) actually reaches the batch result line, rather than being logged nowhere — the plan's own Step 4 asks for this and the original method signature had no channel for it.

**Measured:** `read_formats` over 200 cells with the widened property set (12 COM reads/cell vs. 4 previously) — not independently re-measured against real Excel from here; comment in the code cites ~1.1s, carried over from this fork's own reasoning, not re-verified. Flag for the manual pass.

## Manual verification matrix (none of this has been run — no interactive Office session reachable from here)

- [ ] Existing 4-property call behaves exactly as before.
- [ ] fontName/fontSize/fontColor/strikethrough each apply independently.
- [ ] `underline: true` → single; `underline: 'double'` → double; `underline: 'nonsense'` → specific error.
- [ ] `horizontalAlignment`+`verticalAlignment` both apply.
- [ ] `wrapText: true` wraps and grows the row.
- [ ] `textRotation: 45` rotates; `120` → specific error.
- [ ] `indent: 3` indents; `20` → specific error.
- [ ] `borders: {preset:'all', style:'thin'}` over a multi-cell range → full grid.
- [ ] `borders: {preset:'outline', style:'thick', color:'#FF0000'}` → thick red box only.
- [ ] `borders: {preset:'none'}` → all cleared.
- [ ] `borders: {preset:'all'}` on a single cell → outline only, result text notes the skipped interior edges.
- [ ] `read_formats` round-trips every property above.
- [ ] Actual wall-clock time for a 200-cell `read_formats` — confirm it's comfortably under ~2s; if not, revisit the cap.
- [ ] Natural language: "make the header row bold, centered, white on dark blue, with a thick bottom border" → one correct op.
