# PP-17 — Verification

**Scope:** all 4 tasks done. `AddDefinedName`/`DeleteDefinedName` now accept `scope:'sheet'|'workbook'` (default workbook, unchanged from before). Added: name-syntax validation (digit-start/space/cell-address-like), leading-`=` double-prefix fix, auto-qualification of an unqualified `ref` to the target sheet (quoting sheet names with spaces), `overwrite`-gated collision handling instead of Excel's silent replace, and — on delete — a specific "not found" error that checks the *other* scope and points the model at it when the name exists there instead.

**Automated checks:** `npx tsc --noEmit` clean; `MSBuild ExcelAiAddIn/ExcelAiAddIn.csproj` clean.

**Deviations:** none of substance. `DefinedNameExists` does a linear scan of the `Names` collection (no indexer-by-existence check available without risking the same COM exception this task exists to avoid) — fine at the scale defined names exist in practice.

## Manual verification matrix (none of this has been run — no interactive Office session reachable from here)

- [ ] `add_defined_name({name, ref})` with no scope → workbook-scoped, unchanged from before.
- [ ] `add_defined_name({name, ref, scope:'sheet'})` → Name Manager shows the sheet in Scope.
- [ ] `add_defined_name({name, ref, scope:'sheet', sheet:'Assumptions'})` → scoped to that sheet, not the active one.
- [ ] `ref: '=A1:B10'` → works (not `==A1:B10`).
- [ ] Unqualified `ref` → resolves to the intended sheet even after switching the active sheet.
- [ ] Duplicate name without `overwrite` → specific error; with `overwrite:true` → replaced.
- [ ] Invalid names (`A1`, `1x`, `has space`) → specific errors naming the rule.
- [ ] `delete_defined_name({name, scope:'sheet', sheet:'Assumptions'})` → removes the sheet-scoped one, leaves a same-named workbook-scoped one intact.
- [ ] Deleting a nonexistent name → error names the searched scope and points at the other scope if found there.
- [ ] `read_sheet_features` before/after reflects every change above.
